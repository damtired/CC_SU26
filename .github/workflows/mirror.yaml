on: [push]
      steps:
        - uses: actions/checkout@v3
          with:
            fetch-depth: 0
        - uses: yesolutions/mirror-action@master
          with:
            REMOTE: 'https://github.com/codecollectiveKHM/CC_SU26.git'
            GIT_USERNAME: sarahciston
            GIT_PASSWORD: ${{ secrets.GIT_PASSWORD }}